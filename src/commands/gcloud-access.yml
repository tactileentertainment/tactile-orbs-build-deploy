---
description: >
  Configure access to Google Cloud.

  This expects the follow environment variables
  * `GOOGLE_PROJECT_ID` - The project name
  * `GCLOUD_SERVICE_KEY` - The service account key

steps:
  - run:
      name: Set up Google Cloud access
      command: << include(scripts/gcloud-access.sh) >>
